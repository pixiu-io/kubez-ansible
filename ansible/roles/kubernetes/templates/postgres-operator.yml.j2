apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ postgres_name }}
  namespace: {{ postgress_namespace }}
spec:
  channel: stable
  name: postgres-operator
  source: operatorhubio-catalog
  sourceNamespace: olm
